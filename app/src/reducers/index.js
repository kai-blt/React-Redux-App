//Initialize Program State
export const initialState = {
    test: 'testing'
}

//Reducer
export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}